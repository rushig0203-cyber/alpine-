import AstoniaLanding from "./AstoniaLanding";

/**
 * The public home is a focused, standalone Alpine Astonia launch experience.
 * The portfolio and project routes remain available separately under /projects.
 */
export default function Index() {
  return <AstoniaLanding />;
}
